import React from "react";

function LoginPage() {
  return (
    <div>
      <h2>Login Page</h2>
      <p>Check if this message appears to ensure the login page works.</p>
    </div>
  );
}

export default LoginPage;
